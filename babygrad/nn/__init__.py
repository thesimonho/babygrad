"""Neural-network building blocks.

No re-exports: import directly from the submodule that defines a name, e.g.
``from babygrad.nn.modules import Sequential`` — consistent with the rest of
the package (``babygrad.tensor``, ``babygrad.ops``, ...).
"""
